refactor(docs): docs/ 改名为 assets/，只放 logo 资源

docs/ 里已经没有任何文档了（站点外壳与用户文档都已删除），只剩两个
logo 图片。继续叫 docs/ 会让人以为里面有文档，改名为 assets/ 更贴合
实际用途。

同步更新三处引用：
- st.py 的 page_icon="assets/logo.svg" 与 st.image("assets/logo.png")
- README.md 首页站标 <img src="/assets/logo.png">
- devdocs/01-entrypoints/01-Streamlit主应用入口.md 里对该资源的 4 处描述

顺带撤掉 .gitignore 里上一轮为 docs/ 加的 !docs/ 例外（目录已改名，
该规则已无对象）。

校验：
- st.py / README.md 的资源引用均指向存在的文件
- 全仓库无 docs/logo 残留引用
- 回归测试通过：Ran 174 tests OK (skipped=6)

git mv 保留了文件历史。

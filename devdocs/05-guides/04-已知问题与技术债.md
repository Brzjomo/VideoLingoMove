---
title: 已知问题与技术债
layer: 05-guides
source_files:
 - core/ask_gpt.py
 - core/config_utils.py
 - core/step1_ytdlp.py
 - core/step2_whisperX.py
 - core/step4_1_summarize.py
 - core/step4_2_translate_all.py
 - core/step5_splitforsub.py
 - core/step6_generate_final_timeline.py
 - core/step7_merge_sub_to_vid.py
 - core/all_whisper_methods/volcano_asr.py
 - core/all_whisper_methods/whisperX_utils.py
 - core/all_whisper_methods/transcription_cache.py
 - st_components/task_runner.py
 - st_components/sidebar_setting.py
 - easy_util.py
 - installer.py
 - install.py
 - launch.py
 - requirements.txt
 - .gitignore
 - config.example.yaml
status: verified
last_verified: 2026-09-16
---

# 已知问题与技术债

> **本文件分两部分读**：
> - 「四、仍需你人工处理」是**当前仍然有效**的行动项（轮换密钥、清理 git 历史）。
> - 「二」~「六」是**历史修复记录**（重构 Round 1、Round 2、上游 3.0.4 合并、环境大升级）。
> 这些条目都已落地，保留它们是为了回答"某段代码为什么长这样"。**已修复的问题不再逐条展开**，
> 只保留一行摘要；需要完整原始分析（根因、复现代码）请查 git 历史。
>
> ⚠️ 文中出现的 `core/step8_*`~`step12_*`、`core/all_tts_functions/`、`delete_retry_dubbing.py`
> 等路径**均已在仓库中不存在**（配音链路整体删除），提及它们只是为了记录"为什么删"。

---

## 一、职责与边界

本文只记录**当前仍然存在**的技术债与限制，以及本轮重构的修复记录。功能说明见 `02-pipeline/`；排障步骤见 [`05-常见故障排查.md`](05-常见故障排查.md)。

---

## 二、重构 Round 1 摘要

| 项 | 内容 |
| --- | --- |
| 删除 | `core/step8_1/8_2/9/10/11/12_*.py`、`core/all_tts_functions/`（8 个文件）、`core/delete_retry_dubbing.py`、`third_party/whisperX/`（只剩 `.pyc` 的空壳） |
| 迁移 | `estimate_duration.py` → `core/estimate_duration.py`；`check_len_then_trim` → 新建的 `core/subtitle_trim.py`（原在 `step8_1`，被 `step4_2` 复用） |
| 新增 | `core/subtitle_trim.py`、`config.example.yaml` |
| 配置 | 删除 `Dubbing Settings` 整段与 `dub_volume` / `min_subtitle_duration` / `tolerance`；`speed_factor` 只保留 `max`；新增 `subtitle.align_on_mismatch` |
| 流水线 | 由「字幕 + 配音」两阶段变为**单阶段字幕链路**：step1 → step2 → step3 → step4 → step5 → step6 → step7 |
| 顺带消失的隐患 | `gpt_sovits_tts.py` 的全局 `os.chdir`、`custom_tts` 空实现、`eval` 反序列化、`exit(1)` 绕过重试等 |

---

## 三、已修复清单（Round 1）

### P0 —— 阻断级（全部已修）

| 编号 | 原始症状 | 修法 |
| --- | --- | --- |
| P0-1 | 配音阶段读 `output/audio/*_subs_for_audio.srt`，但生产者被注释，必 `FileNotFoundError` | 配音链路整体删除，问题消失；同时清理了 `step6` 里那段被注释的死代码与假的"已生成"日志 |
| P0-2 | `ask_gpt` 把"业务校验失败"误报成"JSON 解析失败"，且 token 在每次重试都重复累加 | 重写 `core/ask_gpt.py`：解析与校验拆成两个独立分支，错误信息如实透传；token 只在成功拿到响应时统计一次 |
| P0-3 | `valid_def` 失败后的重试是"同一个 prompt 再问一遍" | 重试时把上一次的真实失败原因回注进 messages（`assistant` + `user` 两条），让模型有机会纠正 |
| P0-4 | `demucs: false` 时 `step12` 缺 `background.mp3`，ffmpeg 失败却打印 "successfully merged" | `step12` 已删除 |
| P0-5 | `pause_before_translate` 用 `input`，Streamlit 下阻塞服务 | 改为**两段式 UI 流程**：`process_text` 在术语提取后返回 `True`，页面显示"继续翻译"按钮，用 `st.session_state` 传递暂停状态。⚠️ **该实现随后被「上游 3.0.4 合并」替换**：`process_text` 已删除，现在由 `step_summarize` 调 `TaskRunner.request_review_pause`（`st_components/task_runner.py`）、UI 由 `task_control_panel`（`st.py`）渲染「✅ 术语已确认，继续」。见「六」 |

### P1 —— 高危（全部已修）

| 编号 | 原始症状 | 修法 |
| --- | --- | --- |
| P1-1 | LLM 缓存只按 prompt 匹配，切换模型/改提示词后静默复用旧结果 | 缓存键改为 `(model, prompt)`；`check_ask_gpt_history` 增加 `allow_cache` 参数 |
| P1-2 | `config.yaml` 明文密钥且**已被 git 跟踪** | 新增 `config.example.yaml`（密钥为占位符）；`.gitignore` 加入 `config.yaml`；执行 `git rm --cached config.yaml`。⚠️ **仍需你手动轮换密钥并清理历史**，见「四」 |
| P1-3 | 对齐逐字符精确匹配，一个字符差异即抛错中断 | `get_sentence_timestamps(..., on_mismatch=)` 支持 `warn`（模糊兜底 + 告警）与 `strict`（原行为）；新增配置 `subtitle.align_on_mismatch`，默认 `warn` |
| P1-4 | `output/log/*.xlsx` 的列表列用 `eval` 反序列化（可执行任意代码） | 使用 `eval` 的两个模块（`step10` / `step11`）已删除；全仓库已无 `eval(` 调用 |
| P1-5 | 直通模式下 `terminology.json` 的建立依赖 UI 手写，契约不清 | 抽出 `st.py: ensure_terminology_file`，两条分支都保证文件存在 |
| P1-6 | 提示词要求输出 `topic`，代码读 `theme`，主题上下文恒为空 | 统一为 `topic`：`step4_2` 改读 `topic`，`step4_1` 落盘时补 `topic` 键 |
| P1-7 | `.gitignore` 的 `*.md` 把 `devdocs/` 一起忽略 | 追加 `!devdocs/` 与 `!devdocs/**/*.md`（放在 `*.md` 之后）；已用 `git check-ignore` 验证 |
| P1-8 | 磁盘上的 `None.json` 让「API 密钥检查」命中缓存假通过 | 三处连通性检查（sidebar / batch gui / batch_processor）改为 `log_title=None, use_cache=False`；`save_log` / `check_ask_gpt_history` 用 `_is_no_log` 统一处理 `None` 与 `'None'` |
| P1-9 | 火山引擎分段转写时 `result_start_offset` 被置 0，第 2 段起时间戳与第 1 段重叠 | `volcano_asr.py` 改为 `result_start_offset = start`（片段时间戳从 0 计，必须加回区间起点） |

### P2 —— 中危（全部已修或随删除消失）

| 编号 | 处置 |
| --- | --- |
| P2-1 | 「删除配音文件」按钮与其模块一并删除 |
| P2-2 | `ask_gpt` 的 `None` / `'None'` 语义统一（`_is_no_log`） |
| P2-3 | `find_video_files` 不再因"多个视频"直接抛错：0 个报明确错误，多个则告警并取最新。⚠️ **"选哪个文件"的行为仍有效，但"哪些文件参与候选"的过滤规则当时是错的**（用 `not file.startswith("output/output")`），已由「上游 3.0.4 合并」修好，见「六」 |
| P2-4 | `split_chunks_by_chars(chunk_size, max_i)` 去掉误导性默认值，改为必填 |
| P2-5 | `enhance_vocals` 随配音链路删除（`step2` 不再需要它） |
| P2-6 | `step9` 无条件调用 `demucs_main` 的问题随该文件删除而消失 |
| P2-7 | `reset_total_statistics` 现在会重置 `estimated_total_cost`；新增 `easy_util.add_to_total_cost` |
| P2-8 | `easy_util` 仍是模块级全局状态（见「五」R6），但补了 `ensure_utf8_console` 与入口初始化 |
| P2-9 | `resolution: 0x0` 时页面顶部显示显式警告，说明不会产出真实视频 |
| P2-10 | 假的 "Audio subtitles generated" 日志随死代码清理而消失 |

### P3 —— 低危（绝大部分已修）

| 编号 | 处置 |
| --- | --- |
| P3-1 / P3-38 | zip 打包改为**先收集清单再去重**，用 `_subtitle_zip_name` 做完整文件名匹配（不再用 `"src" in name` 的子串判断）；默认字幕不再有写盘副作用 |
| P3-2 | 死代码 `get_correct_video_file` / `replace_underscore_with_space` 已删除 |
| P3-5 | 移除冗余的 `results.sort`，并就地注释说明 SequenceMatcher 实为自匹配 |
| P3-6 | `step5` 的 `executor.map` 改为 `list(...)` 消费，并捕获单行切分异常打印告警（此前异常被完全吞掉） |
| P3-9 | SRT 生成不再为单列字幕写空行：消除 `\n\n\n`，`split('\n\n')` 解析器不再得到空首行块 |
| P3-10 | `step6` 中未使用的 `words` 变量已删除 |
| P3-11 | `onekeycleanup` 用 `os.path.basename` 取代 `split("/")[1]` |
| P3-19 | `install.py` Windows 分支的 `extra_note` 未定义变量 → 补 `extra_note = ""` 默认值。⚠️ **该文件已在「上游 3.0.4 合并」中改写为 19 行的兼容包装**（`from installer import main`），真实实现在 `installer.py`；新实现用 `ensure_ffmpeg`（`installer.py`）按平台/分支直接给出提示面板，**根本不存在 `extra_note` 变量**，问题随之消失 |
| P3-20 | `download_ffmpeg_windows` 的 `except` 补 `return False`。该函数现位于 `installer.py`，`except` 分支已 `return False`（`installer.py`）；调用方 `ensure_ffmpeg`（`installer.py`）据此走失败分支 |
| P3-21 | `third_party/whisperX/`（只剩 `.pyc` 的空壳）已删除 |
| P3-23 | 空实现的 `custom_tts` 随 TTS 适配层删除 |
| P3-25 / P3-26 / P3-27 / P3-28 | 随 `step8_2` / `step9` / `delete_retry_dubbing` 删除而消失 |
| P3-29 | 批量模式"勾选优先本地计算时静默禁用字幕烧录" → 条件改为只依赖 `preprocess_only` |
| P3-30 | 批量总耗时被算成 2 倍 → 移除 `eu.add_to_total_time` 调用（`step6` 已累加） |
| P3-32 | 两套成本口径并存 → 保留 `get_total_cost`（token 重算）为批处理口径，新增 `add_to_total_cost` 并纳入重置；口径已在 `easy_util` docstring 说明 |
| P3-34 | 每次点「开始批量处理」都 `rmtree` 上一批产物 → 改为归档到 `batch/output_archive_<时间戳>/` |
| P3-35 / P3-44 | `step7` 用 `exit(1)` 抛 `SystemExit` 绕过重试 → 改为 `raise FileNotFoundError(...)` |
| P3-39 | `st.py` 的 PATH 注入每次 rerun 重复追加 → 改为幂等（先检查再追加） |
| P3-40 | `use_column_width` 弃用参数 → 改为 `use_container_width=True`。⚠️ 该改动**只对 `st.button`/`st.dataframe` 等控件成立**，对 `st.image` 是错的（见「五」5.2 的 G4）。当前 streamlit ≥1.49.1 下 `st.image` 用的是 `width="stretch"` |
| P3-42 | `ENHANCED_VOCAL_PATH` 等死常量 → 部分随 `enhance_vocals` 删除；剩余见「五」R10 |
| P3-7 / P3-8 / P3-16 / P3-18 / P3-22 / P3-24 / P3-31 / P3-33 / P3-36 / P3-37 / P3-41 / P3-43 | **未修**，逐条理由见「五」 |

### 本轮新增发现并修复

| 项 | 说明 |
| --- | --- |
| **Windows GBK 控制台崩溃** | 直接 `python core/step6_....py` 会在打印 emoji 时抛 `UnicodeEncodeError: 'gbk' codec can't encode character`。已新增 `easy_util.ensure_utf8_console`，并在 `st.py` 与 `batch/utils/gui.py` 入口调用。⚠️ **覆盖范围当时不够**：`core/` 下的单步入口仍然会崩，已在「上游 3.0.4 合并」中补齐 18 个模块，见「六」 |
| **`record_summary_info` 的荒谬耗时** | 单步运行时 `eu.start_time` 为 0，`end - start` 得到"从 1970 年至今"的秒数（实测打印出 `497099小时...`）。已加兜底：未设置起始时间则记为 0 |
| **SRT 与 `split('\n\n')` 的兼容性** | 与 P3-9 同源；修完后用 75 句真实词级数据端到端验证：`trans.srt` 序号连续、双语文件每块 4 行 |
| **`gpt_sovits_tts.py` 的全局 `os.chdir`** | `start_gpt_sovits_server` 会 `os.chdir` 到 GPT-SoVITS 目录，异常路径下不回退，会让整个进程的工作目录错位（全代码用相对路径，后果严重）。该文件随配音链路删除 |

---

## 四、仍需你人工处理（代码无法代劳）

### 4.1 轮换密钥并清理 git 历史（最高优先级）

`config.yaml` 中的真实密钥**已经进入 git 历史**（多笔提交改过该文件）。代码侧只能做到"以后不再跟踪"，历史仍在：

```powershell
# 1) 立刻到各平台轮换/吊销（这一步只能你来做）
# DeepSeek / 阿里云 DashScope / SiliconFlow / 火山引擎 ASR / 火山 TOS

# 2) 确认现在的状态：config.yaml 已不被跟踪且被忽略
git check-ignore -v config.yaml # 应输出 .gitignore:<n>:config.yaml
git ls-files config.yaml # 应无输出

# 3) 清理历史（会重写提交，需先与协作者协调）
# pip install git-filter-repo
git filter-repo --path config.yaml --invert-paths
# 或使用 BFG；完成后强推并通知所有协作者重新克隆
```

### 4.2 首次运行前填入密钥

`config.yaml` 现在是本地文件（不入库）。本轮**保留了你原有的真实值**，可直接用；在别的机器上克隆后需要：

```powershell
copy config.example.yaml config.yaml
# 然后填入密钥；或用环境变量（见 04-interfaces/01-配置文件与参数.md）
```

---

## 五、Round 2：收尾修复（已全部处理）

Round 1 留下的 15 条（R1~R15）在 Round 2 中逐条处置完毕。下表是处置记录。

### 5.1 原本需要决策或外部条件的（全部已修）

| # | 位置 | 原问题 | Round 2 处置 |
| --- | --- | --- | --- |
| R1 | `batch/utils/tos_manager.py` 与 `core/all_whisper_methods/tos_service.py` | 两份 TOS 实现，`BatchTOSManager` 三个业务方法无调用方 | **合并到 `tos_service.py` 并删除 `tos_manager.py`**。`TOSService` 新增：`get_tos_service` 单例（ASR 分段循环里每段都会新建 `VolcanoASR`，不共享实例会导致文件缓存与统一清理失效）、`upload_file(..., prefix=, name_hint=)` 语义化对象键、`cleanup_by_name`、`get_upload_info`、`reset_tos_service`。`volcano_asr.py` 与侧边栏改用单例，`video_processor` 的 TOS 监控改走 `get_tos_service` |
| R2 | `batch/utils/{video_processor,batch_processor}.py` | `asr_engine: 'volcano'` 时不生成 `for_whisper.mp3`，`prioritize_local` 的缓存恢复必然失败 | 新增 `required_preprocess_files`：**按引擎决定必需清单**（volcano 时不要求 `for_whisper.mp3`）；恢复时改为"只恢复存在的文件"，并打印缺失项以便排查 |
| R3 | `batch/utils/batch_processor.py`（`process_batch` 的 `except`） | 引用可能未赋值的 `status_text` → `NameError` 覆盖真实错误 | 函数开头先 `status_text = None`，`except` 中做存在性判断并包一层 try |
| R4 | `st_components/sidebar_setting.py` + `button_style` | `div[data-testid="stButton"] button { color: white !important }` 命中**全页**按钮，与 `button_style` 的 `color: #144070` 冲突（hover 背景是白） | 作用域限定为 `section[data-testid="stSidebar"] ...`，并补上 hover 规则；主区域按钮恢复深蓝字 |
| R5 | `output/log/asr_results/` | 火山二级 ASR 缓存无清理入口 | `st.py` 在字幕阶段完成后，若检测到该目录有缓存则显示一个折叠区 + 「清空 ASR 结果缓存」按钮 |

> ✅ R1 的合并顺带修掉一个**未记录的潜在缺陷**：`volcano_asr.py` 原来每个音频段都 `TOSService` 新建实例，导致 `file_cache`（"同一文件只上传一次"）在分段场景下失效、且 `uploaded_files` 分散在各实例里无法统一清理。

### 5.2 补充修复：Round 1 引入的字幕保存回归（重要）

| # | 位置 | 问题 | 修法 |
| --- | --- | --- | --- |
| **G1** | `batch/utils/video_processor.py: save_subbtitles` | **Round 1 重写该函数时引入回归**：原版在做 `os.path.isfile(output/<视频名>.srt)` 判断**之前**，会在 zip 循环里调用 `copy_as_default_subbtitle` 把 `trans_src.srt` 复制成 `output/<视频名>.srt`。重构时那次调用被移除、但"先判断后写"的顺序被保留，于是：① `output/<视频名>.srt` 从未生成 → 归档到 `batch/output/<视频名>/` 时也就没有它；② 复制到输入目录的 `os.path.isfile` 判断恒为 False → 输入目录拿不到字幕。**zip 本身是正常的**（它直接用 `trans_src.srt`），所以只有"磁盘上的默认字幕"受影响 | 重构函数，明确三段式：**① 先写 `output/<视频名>.srt` 与 `<视频名>.txt` → ② 再打包 zip → ③ 最后复制到 `INPUT_DIR`**。每步都打印实际结果（生成/复制/打包条目数），失败时有明确告警而不是静默跳过 |
| **G2** | `st_components/imports_and_utils.py: download_subtitle_zip_button` | 同源问题：原版在 zip 循环里顺带写盘 `output/<视频名>.srt`，重构后只放进 zip、不再落盘（**单次模式**下 output/ 里没有与视频同名的字幕） | 显式恢复该落盘行为（单次模式与批处理行为一致） |
| **G3** | `st_components/imports_and_utils.py: subtitle_zip_name` | 该函数被 `batch/utils/video_processor.py` 通过 `from st_components.imports_and_utils import *` 使用，但原命名带下划线前缀（`_subtitle_zip_name`），**通配导入不会导入下划线开头的名字** → 批处理保存字幕时 `NameError` | 重命名为 `subtitle_zip_name`（不带下划线），保留 `_subtitle_zip_name` 作为显式 import 的兼容别名 |
| **G4** | `st.py: main` 的 `st.image` | Round 2 按 P3-40 的"建议"把 `use_column_width=True` 改成了 `use_container_width=True`，但**这个参数名只适用于 `st.button` / `st.dataframe` / `st.download_button` 等控件，`st.image` 在当时的 streamlit 1.38.0 用的是 `use_column_width`**。结果普通模式一打开就崩：`TypeError: ImageMixin.image got an unexpected keyword argument 'use_container_width'` | 改回 `use_column_width=True`，并把"两者适用范围不同"写进注释。⚠️ **该结论只对 1.38.0 成立**：环境大升级后 streamlit ≥1.49.1，`st.image` 的正确写法已改为 `width="stretch"`（`use_column_width` 自 1.40 起弃用） |
| **G5** | `core/step7_merge_sub_to_vid.py` 的 `resolution == '0x0'` 分支 | **占位视频用错了场景**。原实现无条件生成 1 秒黑帧 `output_sub.mp4`：(a) 输入是真实视频时，这个文件毫无业务价值（没字幕、也没被任何代码消费），却以"成片存在"骗过了 UI 的阶段完成判据；(b) 输入是**纯音频**时更糟——`convert_audio_to_video` 已经把音频包成 `output/black_screen.mp4`（黑底 + 完整音频，时长与音频一致），它本身就是一份可播放的成片，却被 1 秒黑帧覆盖，用户**再也无法播放来校对字幕与音频的对齐** | 按输入类型分派：**纯音频**时把 `black_screen.mp4` 原样复制为 `output_sub.mp4`（保住音频与时长）；**真实视频**时**完全不生成**占位视频（并清理上一次遗留的文件）。为了让 UI 不因"成片不存在"而永远卡在按钮上，新增显式完成标记 `output/log/subtitle_stage_done.txt`，`st.py` 改用它判断阶段完成；顺带把 ffmpeg 失败从"只打印 ❌ 但当作成功"改为 `raise`（returncode 检查） |

> 🔎 **G4 的教训**：P3-40 当初把这条记为"使用旧参数名，升级后需改"，Round 2 按建议直接改名——但**没有核对当前固定版本的签名**就改了。凡是"未来版本才需要"的改动，都必须先用 `inspect.signature` 对着实际安装版本确认，否则就是把一个无害的弃用警告换成一个致命错误。
>
> 🔎 **G5 的教训**：`resolution=0x0` 同时承担了两个不相干的职责——"跳过压制以省时间"与"给纯音频输入造一个载体"。占位视频只对后者有意义；把它当成通用逻辑就会破坏前者。现在通过 `core/step1_ytdlp.is_audio_placeholder` 显式区分这两种输入。

> 🔎 **这两个回归说明了一个值得记住的教训**：`save_subbtitles` 里的副作用是**顺序敏感**的——"生成默认字幕"既是 zip 的来源、也是复制到输入目录的前提。重构时把副作用移除、却保留了依赖该副作用的守卫条件，是最容易漏的一类错误。现在函数开头的 docstring 已写明顺序要求。

### 5.2b 断句部分的 LLM 浪费与独立开关

| # | 位置 | 问题 | 处置 |
| --- | --- | --- | --- |
| **R16** | `core/step5_splitforsub.py` | **直通模式下的无谓自对齐**：`transcription_only=True` 时 `Source` 与 `Translation` 是同一份文本（`step4_2` 直通分支把源文复制给译文），但 `split_align_subs` 仍会对每条超长字幕调 `align_subs` —— 等于"让 LLM 把一段文本与它自己对- 齐"，结果必然等于输入。实测这段白花 **30 次调用** | 新增 `identical_src_trans = src_lines == tr_lines` 判断：文本相同时只调 LLM 切源文，译文用 `split_text_evenly` 按行数机械等分（拼接回原文保真，已用 4 文本 × 4 分段验证）。step6 的对齐只需 Source 拼串精确匹配，等分不破坏它 |
| **R17** | `core/step3_2_splitbymeaning.py`、`core/step5_splitforsub.py` | **断句优化无法关闭**：`transcription_only` 只承诺跳过翻译，但 `step3_2`（按句意切分）与 `step5`（超长行切分）仍会调 LLM。用户"只要字幕"时这部分 token 无法省 | 新增配置 **`llm_sentence_split`** + 侧边栏开关「使用 LLM 优化断句」。**生效规则：翻译模式下强制开启（开关不显示），只有「只生成原语言字幕」时才允许关闭** （译文与源文长度差异大，不做按意群断句会影响双语对齐与单行长度达标）。关闭后 `step3_2` 直接输出 spaCy 结果、`step5` 用新增的 `split_by_punctuation` 按标点就近断开，**整条链路零 LLM 调用**。判定与执行统一走 `core.config_utils.use_llm_sentence_split` 单一入口，避免 UI 与代码漂移 |

**实测调用数对照**（同一份真实数据）：

| 模式 | step3_2 | step5-split | step5-align | 合计 |
| --- | --- | --- | --- | --- |
| 直通 + 断句优化开 | 25 | 67 | **0**（原为 30） | 92 |
| 直通 + 断句优化关 | 0 | 0 | 0 | **0** |
| 翻译 + 断句优化开 | 25 | 30 | 30 | 85 |
| 翻译 + 断句优化关 | 0 | 0 | 0 | **0** |

> `step5-align` 在"翻译"模式下是**必要的**（要按切分后的源文重新对齐译文），不可省；只在"直通"模式下才是浪费。
> `step5-split` 在直通模式下变成 67 是因为切分粒度变了（句子更短、更接近字面上限），这属于用 token 换断行质量，由 `llm_sentence_split` 控制。

### 5.2c 仅转录模式下的默认字幕来源

| # | 位置 | 问题 | 处置 |
| --- | --- | --- | --- |
| **R18** | `st_components/imports_and_utils.py`、`batch/utils/video_processor.py` | **默认字幕固定取 `trans_src.srt`**（双语版）。但仅转录模式（`transcription_only`）下根本没有译文，此时"默认字幕"本应是 `src.srt`。虽然直通模式下 `trans_src.srt` 的内容与 `src.srt` 相同（译文=原文），但语义是错的；若目录里残留了上一次翻译模式的双语字幕，默认字幕还会给出**错误内容** | 抽出 `pick_default_subtitle` 统一判定：<br>• 仅转录 → `src.srt`（缺则退回 `src_trans.srt` → `trans_src.srt`）<br>• 翻译 → `trans_src.srt`（缺则退回 `trans.srt`）<br>两处调用点（UI 下载按钮、批处理 `save_subbtitles`）都改用它 |

### 5.3 属于"设计如此"或影响很小的（全部已修）

| # | 位置 | Round 2 处置 |
| --- | --- | --- |
| R6 | `easy_util` 的模块级全局状态 | **保留设计**（改 `st.session_state` 会破坏批处理依赖），但已补齐文档说明；本轮新增的 `ensure_utf8_console` 也放在这里，属于入口级工具 |
| R7 | `core/step4_2_translate_all.py` 直通模式读入未使用 | **保留**：它同时承担"直通模式仍要求 `cleaned_chunks.xlsx` 存在"的隐式校验，删除会改变行为；已在代码注释中说明 |
| R8 | `calc_len` 权重与注释不一致 | 修正第 23 行注释，使其与代码一致（中日/全角 1.75、韩 1.5、泰 1、其他 1） |
| R9 | `plyer` 桌面通知可能中断 `step6` | `send_tanslation_complete_notification` 包 try/except 并打印告警；无桌面环境不再影响结果 |
| R10 | `VOLCANO_FILE` / `convert_to_volcano_wav` 死代码 | 删除 `VOLCANO_FILE`、删除未被调用的 `convert_to_volcano_wav`、清理相应 import。`ENHANCED_VOCAL_PATH` **保留**（`enhance_vocals` 在 `demucs=true` 时真的用它），并修正了注释 |
| R11 | 环境变量覆盖对 UI 是只读的 | `sidebar_setting.config_input` 现在会检测 `VIDEOLINGO_<KEY>`：**显示提示说明该字段被环境变量覆盖、此处编辑不生效**，并在用户尝试修改时给出 warning（不再静默丢弃输入） |
| R12 | `install.py` 的 `runtime/python` PATH 注入 | 删除该无用注入（目录在仓库中不存在），并留注释说明为何移除 |
| R13 | `step5` 的 `attempt` 只用于打印 | 引入 `MAX_SPLIT_ATTEMPTS` 常量，日志改为 `attempt N/MAX`，语义明确 |
| R14 | `prompt + ' ' * retry` 绕缓存 | `ask_gpt` 新增显式 `bypass_cache` 参数（只跳过读取、仍写回结果）；`step3_2_splitbymeaning` 与 `translate_once` 改为"首次允许命中缓存、重试时 `bypass_cache=True`"，日志里的 prompt 不再被空格污染 |
| R15 | `batch/README.zh.md` 的「重新检查API」按钮 | 更新为实际存在的操作（侧边栏 📡 按钮），并补充任务表列变更说明 |

### 5.4 仍然保留的"设计取舍"（非缺陷，改动会改变行为）

| # | 位置 | 说明 |
| --- | --- | --- |
| K1 | `core/step4_2_translate_all.py` 的 SequenceMatcher 回填 | `translate_lines` 的第二个返回值就是传入的 chunk，因此该匹配实为自匹配（ratio 恒 1.0），`<0.9` 抛错分支不可达。保留它是为了未来 `translate_lines` 改为返回不同文本时仍能对齐；已在代码注释中写明 |
| K2 | `easy_util` 模块级全局状态 | 见 R6 |
| K3 | 直通模式下的 `cleaned_chunks.xlsx` 依赖 | 见 R7 |

---

## 六、上游 3.0.4 合并（历史：合并分支，已删除）的修复与新增债

本节只登记**本次合并**造成的状态变化；合并的取舍理由见
上游合并记录。

### 6.1 本次修掉的缺陷（逐条给出源码定位）

| # | 缺陷 | 位置 | 修法（已读代码确认） | 与本文件原有条目的关系 |
| --- | --- | --- | --- | --- |
| M1 | 侧边栏只写 `whisper.language`，不同步 `detected_language`；下游提示词与 spaCy 模型加载读的却是 `detected_language`（`core/spacy_utils/load_nlp_model.py` 现已改用 `get_source_language`），切换识别语言后语种错配 | `st_components/sidebar_setting.py`、`core/config_utils.py` | 新增唯一判定点 `get_source_language`（`core/config_utils.py`）：`whisper.language` 是明确语言时一律以它为准，只有它是 `auto`/空才回退 `detected_language`；`update_key` 写 `whisper.language` 时**原子同步** `detected_language`（`core/config_utils.py`，选 `auto` 时跳过）。侧边栏在火山引擎下还会一并同步 `volcano_asr.language`（`sidebar_setting.py`）。回归测试 `tests/test_source_language.py`（5 例） | 新增条目（原清单未记录） |
| M2 | 火山 ASR 分支从不写 `whisper.detected_language`，自动检测模式下下游读到上一个视频的旧语言 | `core/step2_whisperX.py` | 火山分支补 `save_language(result.get('language'))`（`core/step2_whisperX.py`） | 新增条目 |
| M3 | `_detect_language_from_result` 在自动检测模式下**无条件返回 `"en"`**（伪造语言，比"未知"更糟） | `core/all_whisper_methods/volcano_asr.py` | 改为如实回报：先取响应里的 `language`/`lang`/`detected_language` 并归一化为 ISO-639-1，其次用配置里显式指定的语言，都拿不到就返回 `None`（`volcano_asr.py`）。`save_language` 本身也会过滤 `None`/空串/`'auto'`（`whisperX_utils.py`） | 新增条目 |
| M4 | `find_video_files` 用 `not file.startswith("output/output")` 过滤，**两个方向都错**：误删 `output tutorial.mp4`，误收 `output/OUTPUT_SUB.MP4` | `core/step1_ytdlp.py` | 改为按**文件名白名单**排除自己生成的成品：`GENERATED_VIDEO_NAMES = {"output_sub.mp4", "output_dub.mp4"}` + `basename.lower` 比较（`core/step1_ytdlp.py`）。`black_screen.mp4` 仍是合法源文件 | **修正 P2-3**（多文件行为没变，过滤规则是错的） |
| M5 | `raw.mp3` 声明 32kHz/128kbps，实际由 16kHz 的 `raw.wav` 转码而来，带宽被卡在 8kHz 以内，参数是假的 | `core/all_whisper_methods/whisperX_utils.py` | `convert_video_to_audio` 的两条命令都改为**直接从源媒体**编码，并加 `-af aresample=async=1:first_pts=0` 对齐时间轴（`whisperX_utils.py`）。回归测试 `tests/test_audio_extraction.py` 用 12kHz 正弦实测高频保留 | 新增条目 |
| M6 | 无 GPU / macOS 分支**打印**"安装 CPU 版 PyTorch"却执行与 GPU 分支完全相同的 cu118 命令 | `install.py` | `installer.install_torch`（`installer.py`）真的按硬件选源：有 CUDA 用 `CUDA_INDEX`（`installer.py`）或本地 whl，否则用 `CPU_INDEX`（`installer.py`）。`detect_cuda_version`（`installer.py`）改用 `nvidia-smi` 解析，不再依赖已废弃的 `pynvml` | **修正 P3-19** 所在文件 |
| M7 | `install_requirements` 吞掉 `CalledProcessError`，依赖装失败仍报"完成" | `install.py` | `installer.run` 失败会重试（3/6/9…秒退避）后 `raise SystemExit`（`installer.py`）；`pip` 默认 `check=True`（`installer.py`）；`--check` 的退出码为错误条数（`installer.py`） | 新增条目 |
| M8 | Linux 装 `fonts-noto`（**不含**中日韩字形），中文字幕仍是方块 | `install.py` | `installer.install_cjk_fonts`（`installer.py`）按发行版装真正的 CJK 包：`fonts-noto-cjk` / `google-noto-sans-cjk-fonts` / `noto-fonts-cjk`（`installer.py`），并用 `fc-match` 先探测、装完 `fc-cache -f` | 新增条目 |
| M9 | Windows GBK 控制台下 `python -m core.step2_whisperX` 这类单步命令打印 emoji 抛 `UnicodeEncodeError` | 18 个带 `__main__` 的 core 模块 | 每个入口补 `easy_util.ensure_utf8_console`（示例 `core/step2_whisperX.py`；`easy_util.py` 定义）。覆盖 `core/` 顶层 11 个（`step1_ytdlp`、`step2_whisperX`、`step3_2_splitbymeaning`、`step4_2_translate_all`、`step5_splitforsub`、`step6_generate_final_timeline`、`step7_merge_sub_to_vid`、`translate_once`、`json_to_subtitle`、`onekeycleanup`、`pypi_autochoose`）+ `core/spacy_utils/` 4 个 + `core/all_whisper_methods/` 3 个（`volcano_asr`、`demucs_vl`、`tos_service`），另加 `installer.py`、`launch.py` | **补齐**「本轮新增发现并修复」中的 GBK 条目 |
| M10 | `config.example.yaml` 里 `transcription_only` 重复出现两次，YAML 后键覆盖前键，语义取决于行序 | `config.example.yaml` | 现在只出现一次（`config.example.yaml`），并在上方注释里写明"此处只能出现一次" | 新增条目 |
| M11 | `check_api` 被调用两次，而该函数刻意绕过缓存（`use_cache=False`）→ 每次点 📡 发两次真实请求 | `st_components/sidebar_setting.py` | 只调用一次（`sidebar_setting.py`），并把"必须绕过缓存"的原因写进 `check_api` 的 docstring | 新增条目 |

### 6.2 本次合并**新引入**的技术债与验证盲区

| # | 内容 | 位置 / 性质 |
| --- | --- | --- |
| N1 | **本机没有可用的项目 Python 环境**（无 `videolingo` conda 环境，`torch` / `whisperx` / `spacy` / `json_repair` 等均未安装），因此**未做端到端跑通验证**。`python installer.py --check` 在本机就是失败态（退出码 1） | 验证盲区，环境事实 |
| N2 | 未验证 Streamlit 页面的实际渲染（`st.fragment` 进度面板、暂停/停止按钮、术语确认界面）。这些改动只做过静态核对 | 验证盲区 |
| N3 | 未验证火山 ASR 的真实请求链路（无凭据，且会产生费用） | 验证盲区 |
| N4 | **转录缓存跨 `output/` 清理存活**（`.cache/asr/`，`.gitignore:159`）：这是刻意设计（归档 `output/` 不会让缓存失效），但代价是"清理了 output 却仍复用旧识别结果"。UI 侧有清理入口（`st.py`），命令行下只能手工删 `.cache/asr` | 设计取舍 |
| N5 | **缓存身份依赖包版本**：`cache_key` 把 `whisperx` / `faster-whisper` / `demucs` 的版本与 `SCHEMA`（当前为 1）一起算进键（`transcription_cache.py`）。升级这些包会让全部缓存自动失效并重跑（正确但耗时）；反之，**改了 `_asr_cache_settings` 没覆盖到的参数不会失效** | 设计取舍 + 易踩点 |
| N6 | 状态指纹文件写在 **Python 环境目录**（`Path(sys.prefix)/.videolingo-install.json`，`installer.py`），不在仓库内。同一环境换仓库目录仍会命中"已安装"而跳过 | 设计取舍 |
| N7 | `OneKeyStart.bat` 仍然硬编码 conda 路径 `%USERPROFILE%\anaconda3\envs\videolingo`（`OneKeyStart.bat`）。Anaconda 装在非默认位置时它只会打印"[错误] 未找到 conda 环境"，仍需手工改脚本 | 未修（有明确提示，比静默失败好） |
| N8 | `installer.py` 与 `install.py` **两个入口并存**：`install.py` 是 19 行的兼容包装（默认追加 `--launch`）。文档/脚本若混用两者，`--check` 之外的参数传递行为需要看 `install.py` | 设计取舍 |
| N9 | `tests/` 用标准库 `unittest`（`pytest` 不在 `requirements.txt` 中），因此**没有 CI 集成**，也没有覆盖率统计。运行方式只有手工 `python -m unittest discover -s tests -v` | 验证盲区 |
| N10 | 3.0.4 合并**刻意不做环境大升级**（当时 torch 仍 2.1.2+cu118、whisperx 仍 v3.2.0、streamlit 仍 1.38.0），所以上游那批依赖新 torch 的改动（`demucs` 改 PyPI、`setup_env.py`、`runtime_libraries.py`）都没有跟进；`requirements.txt` 的 `pytorch-lightning`/`lightning` pin 也因此**保留未验**（由 pyannote 传递引入，无环境可验证） | 已知限制 |

### 6.2b 本次核对中新发现（**非**本次合并引入，一并登记避免再被漏掉）

| # | 内容 | 位置 | 影响 |
| --- | --- | --- | --- |
| D1 | `opencv-python==4.10.0.84` 已**无任何代码引用**（全仓库 grep 无 `import cv2` / `cv2.`）：旧引用点 `step7` 的黑帧生成在 Round 2 的 G5 中改写掉、`step12` 在 Round 1 删除 | 声明在 `requirements.txt`；仍被 `installer.py` 的 `REQUIRED_IMPORTS` 当作必需包 | 白装一个几十 MB 的包；且没装它会让 `python installer.py --check` 报错并返回 1 |
| D2 | ~~`installer.install_torch(force=False)` 的 `force` 形参从未使用~~ | `installer.py` | **✅ 已修**：形参与其调用处的实参一并删除（`installer.py`）。`--force` 仍只影响"是否跳过整个安装段"，语义不再有歧义 |
| D3 | ~~Python 闸门允许 3.10–3.12，但本地 wheel 名写死 `cp310`，Windows + CUDA 分支只判断文件存在~~ | `installer.py` | **✅ 已修**：① `LOCAL_TORCH_WHEEL` 的解释器标签改为按运行中的 Python 生成（`_PY_TAG`，`installer.py`）；② 闸门为 **3.10–3.13**（`installer.py`）—— 经 PyPI 元数据核实，`torch==2.1.2` 只发布 cp38/39/310/311 wheel，**没有 cp312**，原先允许 3.12 会让 pip 在装 torch 时报"找不到匹配发行版" |
| D4 | ~~`--yes` 参数被解析但没有任何分支读取~~ | 原 `installer.py` | **✅ 已修**：该参数已删除，并在原位留注释说明"本脚本全程非交互，故不提供 `--yes`"。`--help` 中不再出现 |

### 6.3 本次合并**没有**处理的原有条目

「五」里列出的未修项（P3-7 / P3-8 / P3-16 / P3-18 / P3-22 / P3-24 / P3-31 / P3-33 / P3-36 / P3-37 / P3-41 / P3-43）
与 5.4 的三条"设计取舍"（K1 / K2 / K3）本次**均未触碰**，结论不变。其中 K1 现在有了更明确的注释
（`core/step4_2_translate_all.py`）与解释（`core/step4_2_translate_all.py` 的 `topic` 说明）。

---

## 七、验证记录

### 上游 3.0.4 合并（本次）

本次文档核对中**实际执行过**的命令与结果：

| 验证 | 方法 | 结果 |
| --- | --- | --- |
| 回归测试 | `python -m unittest discover -s tests -v` | **Ran 46 tests … OK**（`test_source_language` 5、`test_media_input` 10、`test_transcription_cache` 12、`test_prompt_contract` 12、`test_audio_extraction` 7） |
| 单模块测试 | `python -m unittest tests.test_transcription_cache` | Ran 12 tests … OK |
| 安装体检 | `python installer.py --check --quiet` | 退出码 **1**，打印 8 条缺包错误（`torch` / `torchaudio` / `whisperx` / `spacy` / `cv2` / `librosa` / `pydub` / `json_repair`），与"本机无项目环境"一致 |
| 静态核对 | 逐文件读源码确认函数名、行号、配置键 | 见 6.1 的「位置」列 |

未验证项见 6.2 的 N1~N3（无项目环境、未验证页面渲染、未验证火山真实请求）。

### 补充修复（断句开关与自对齐优化）

| 验证 | 方法 | 结果 |
| --- | --- | --- |
| 三种模式调用数 | 打桩 `ask_gpt`，跑真实 `step5` | 直通+开=92（align 0）、直通+关=0、翻译+开=85 |
| 关开关后零调用 | 把 `ask_gpt` 替换为"调用即抛 AssertionError" | step3_2 与 step5 全程未触发 |
| 机械切分保真 | `split_text_evenly` 4 文本 × 4 分段；`split_by_punctuation` 3 组含无标点超长串 | 拼接回原文均保真，最长段未超上限 |
| 端到端（关开关） | 真实数据跑 step3_2 → step5 → step6 | spaCy 384 句原样输出；434 条字幕；SRT 序号连续、格式异常块 0；零 LLM 调用 |

### 补充修复（字幕保存回归）

用用户实际的视频名 `2 - Collecting an Inspiration Board` 做了完整周期测试（`save_subbtitles(True)` → `cleanup('batch/output')`）：

| 落点 | 期望 | 结果 |
| --- | --- | --- |
| `batch/output/<视频名>/` | 含 `<视频名>.srt`、`<视频名>.txt`、`output_sub.mp4`、`src/trans/src_trans/trans_src.srt`、`log/`、`gpt_log/`、`audio/` | ✅ 全部存在 |
| `batch/input/`（输入视频目录） | 含 `<视频名>.srt` 与 `<视频名>.txt`（作为"已翻译"判据） | ✅ 均存在（27,066B / 9,804B） |
| `batch/output/SavedSubbtitles/` | `<视频名>_subtitles.zip`，6 个条目且无重名 | ✅ 6 条目、无重名 |
| 单次模式 `output/` | 含 `<视频名>.srt` 与 `.txt` | ✅ 已恢复该落盘行为 |

另外用真实数据端到端复跑了字幕链路（step3_2 → step5 → step6 → step7）：

| 步骤 | 结果 |
| --- | --- |
| step3_2 | 216 句（命中 `gpt_log` 缓存，未产生新的 LLM 调用） |
| step5 | 完成，超长行已切分 |
| step6 | 4 个 SRT 均生成，**238 条、序号连续、无兜底告警**（说明精确对齐全部成功） |
| step7 | 真实成片 `output_sub.mp4` 生成成功（7:24，111.5 MB，ffmpeg 17 秒） |
| 普通模式启动 | 用 `streamlit run st.py --server.headless true` 起服务 + 请求首页触发脚本执行：`/_stcore/health` 返回 `ok`，首页 HTTP 200，**服务端无 "Uncaught app exception"**（G4 修复前后对比：修复前 `st.image` 抛 `TypeError`） |
| Streamlit API 兼容性 | 静态扫描全仓库 179 处 `st.*` 调用，逐个与 streamlit 1.38.0 的 `inspect.signature` 比对关键字参数名与位置参数个数 | 全部一致（这项检查已可用于防止 G4 类问题复现） |
| G5 场景 1：真实视频 + `0x0` | 造一个普通 mp4 作源，`resolution=0x0` 跑 step7 | 不生成 `output_sub.mp4`（符合预期）；源视频保留；打印"已跳过视频压制"提示；完成标记写出 |
| G5 场景 2：纯音频 + `0x0` | 用 ffmpeg 造 `black_screen.mp4`（黑底 + 3 秒 440Hz 音频轨）跑 step7 | `is_audio_placeholder` 正确识别；`output_sub.mp4` 生成且**字节与源文件一致**；ffprobe 时长 **3.0 秒**（不是 1 秒）、音频轨编码 `aac`（保留住了音频） |

> 测试期间为生成真实成片曾临时把 `resolution` 设为 `640x360`，**已改回 `0x0`**（保留用户原有的调优配置）。

### Round 2（收尾修复）

| 验证 | 方法 | 结果 |
| --- | --- | --- |
| 语法 | 38 个 `.py` 全部 `ast.parse` | 通过 |
| 导入 | 30 个模块（含 `batch.utils.*`、`AudioExtract`）`importlib.import_module` | 30/30 通过 |
| 配置键完整性 | 静态扫描全部 `load_key('...')` 与 `config.yaml` 比对 | 47 个键全部存在 |
| TOS 合并行为 | 单例、对象键命名、`_safe_name` 清洗、`cleanup_by_name` 空输入 | 单例生效；无 hint 生成 `asr-audio/<ts>_<uuid>.wav`，有 hint 生成 `asr-audio/我的视频_<ts>_<uuid8>.wav`；批量前缀可用；清洗 `a/b\c:d*e?f 中文名` → `a_b_c_d_e_f_中文名` |
| R2 修复 | `VIDEOLINGO_ASR_ENGINE=volcano` 下调用 `required_preprocess_files` | 返回 `['raw.mp3', 'cleaned_chunks.xlsx']`，`for_whisper.mp3` 已移除 |
| 批处理导入健壮性 | `import batch.utils.batch_processor`（不依赖 streamlit 脚本目录） | 修复前 `ModuleNotFoundError: video_processor`，修复后 OK |
| 对齐兜底回归 | 真实词级数据 300 句，破坏第 100 句 | `warn` 无异常；`strict` 正确抛错；干净输入两模式均通过 |
| SRT 端到端回归 | 75 句真实词级数据跑完 `align_timestamp_main` | 4 个 srt 均生成；`trans.srt` 75 条、序号连续 |

### Round 1（删除配音 + P0~P3 修复）

| 验证 | 方法 | 结果 |
| --- | --- | --- |
| 环境变量覆盖 | `VIDEOLINGO_API_KEY` / `_MAX_WORKERS=7` / `_TRANSCRIPTION_ONLY=false` | 分别得到 str / int 7 / bool False |
| 配置自举 | `VIDEOLINGO_CONFIG` 指向不存在的文件 | 自动从 `config.example.yaml` 创建并可读 |
| 已删模块残留引用 | 全仓库 grep `step8_`~`step12_`、`all_tts_functions`、`delete_retry_dubbing`、`tos_manager` | 无残留 |
| 批处理模板一致性 | `tasks_setting-template.xlsx` 与代码期望列比对 | 均为 `Video File / Source Language / Target Language / Status` |
| UTF-8 控制台 | 不设 `PYTHONIOENCODING` 打印 emoji | 未修复前崩溃；调用 `ensure_utf8_console` 后正常 |

---

## 十、相关文档

- [`05-常见故障排查.md`](05-常见故障排查.md) — 按现象反查
- 上游合并记录 — 本次合并的移植清单、刻意不合项与验证边界
- [`../06-batch-and-tools/02-安装脚本与依赖.md`](../06-batch-and-tools/02-安装脚本与依赖.md) — `installer.py` / `install.py` / `requirements.txt` 的现状
- [`../00-overview/02-数据流与中间产物.md`](../00-overview/02-数据流与中间产物.md) — 产物契约
- [`../03-subsystems/01-LLM调用与提示词.md`](../03-subsystems/01-LLM调用与提示词.md) — `ask_gpt` 重写后的行为
- [`../04-interfaces/01-配置文件与参数.md`](../04-interfaces/01-配置文件与参数.md) — 环境变量覆盖与优先级说明
